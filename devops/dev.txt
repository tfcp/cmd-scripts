## 开发细节
对于公司级别的Devops平台开发 crd的封装是非常必要的
1. 屏蔽底层k8s细节
    (降低使用成本)
2. 打包关联资源, 避免更新,删除等脱节
    (一个应用包含的deploy,clb,hpa,service,pvc等都应该打包关联到一起)

海量pod的发布(几千上万)
1. 走滚动部署 会需要很多额外资源做滚动更新(不会丢包)
2. 走内部组件更新(agent)重启 会有流量负载问题(高峰期可能导致丢包)

关于devops的建设
1.
