1. 容器io没有做隔离导致的问题

2. request和limit放开导致的调度碎片问题(调度效率低下)
crd如何修复

3. 公有云selectorSpread优选失效问题

4. HPA精细化

5. start.sh问题 收不到term信号

6. eni弹性网卡问题

7. GPU调度问题

8. 长链接服务异常(grpc)
    1. 滚动更新导致的负载不均衡
        新起来的pod接收到负载更高 后续起来的负载逐步降低
    2. 扩容失效
        链接固定, 扩容也不会有啥负载

9. 如何缩容指定的pod
    1. pod缩容的优先级
        pending > PodUnknow > PodRunning
    2. 非ready状态pod先被删除
    3. 如果pod都是ready状态, 最后变成ready的pod优先被缩容
    4. 重启次数多的优先被缩容
    5. 最新被创建的pod被删除

10. 子网ip不足问题

11. 核心组件scheduler

12. 云原生开发包含什么(所谓的二开 主要开发些什么)

13. 大集群对于网络带宽的挑战
    镜像仓库(一次扩容重建上千个pod)
    存储系统
    在特定场景下(加入一批新节点 然后扩容的时候 比较容易遇到)



